import React from 'react';

const NotFoundBook = async () => {
    // const {blogID} = await params;
    return (
        <div>
            blog post  not found!
        </div>
    );
};

export default NotFoundBook;