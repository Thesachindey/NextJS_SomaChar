const Todos3 = async () => {
    
  await new Promise((resolve) => setTimeout(resolve, 3000));
    return (
        <div>
            todos3 component!
        </div>
    );
};

export default Todos3;